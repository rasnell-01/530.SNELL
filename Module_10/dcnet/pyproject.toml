[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dcnet-starter"
version = "0.1.0"
description = "Starter project for simulating data center networking issues"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
